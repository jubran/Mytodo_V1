default_platform(:ios)

platform :ios do
  desc "Upload IPA to TestFlight with automatic version bump and release notes"
  lane :upload do |options|
    api_key = app_store_connect_api_key(
      key_id: ENV["APP_STORE_CONNECT_API_KEY_ID"],
      issuer_id: ENV["APP_STORE_CONNECT_API_ISSUER_ID"],
      key_content: ENV["APP_STORE_CONNECT_API_KEY"],
      is_key_content_base64: true
    )

    # Increment build number automatically
    increment_build_number(
      xcodeproj: "ios/MyTodo.xcodeproj"
    )

    # Generate release notes from last commit
    last_commit = `git log -1 --pretty=%B`.strip
    release_notes = "Update: #{last_commit}"

    upload_to_testflight(
      api_key: api_key,
      ipa: options[:ipa] || "app.ipa",
      skip_waiting_for_build_processing: true,
      changelog: release_notes
    )
  end
end